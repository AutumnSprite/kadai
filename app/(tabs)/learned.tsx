import { Text, View } from "react-native";

export default function Learned() {
	return (
		<View style={{ flex: 1, paddingTop: 60, alignItems: "center" }}>
			<Text>Learned cards</Text>
		</View>
	);
}